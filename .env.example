# Glance Configuration
# Copy this file to .env.local to customize (all settings are optional!)

# =============================================================================
# OPTIONAL - Security
# =============================================================================

# Encryption key for credential storage (AES-256-GCM)
# If not set, Glance auto-generates and persists one in data/.auth_token
# Only set this if you need to use the same key across multiple instances
# Generate with: openssl rand -base64 32
# AUTH_TOKEN=your-secure-random-token-here

# =============================================================================
# OPTIONAL - Database
# =============================================================================

# Path to SQLite database (defaults to ./data/glance.db)
# DATABASE_PATH=/path/to/your/glance.db